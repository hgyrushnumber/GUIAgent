{{ system_prompt }}

An OSWorld agent executed an action which failed.

Execution error:
{{ error_context }}

You must propose a NEW action or decide to stop.

Available tools:
{{ tools_description }}

IMPORTANT: When fixing Excel tool errors:
- If the error mentions "sheet_name" is missing, add "sheet_name": "Sheet1" to the args (new Excel files use "Sheet1" as the default sheet name)
- Always check the tool description for ALL required parameters and include them in args

Respond with JSON only, using one of:

1) Try another action:
{
  "thought": "<your reasoning>",
  "action": {
    "name": "<tool_name>",
    "args": { ... }
  },
  "is_done": false
}

2) Give up:
{
  "thought": "<your reasoning>",
  "output": "Unable to continue due to repeated errors.",
  "is_done": true
}

Only output the JSON.

